---
import { getCollection, render, type CollectionEntry } from "astro:content";
import ArticleLayout from "../../layouts/ArticleLayout.astro";
import { isContentVisible, slugFromId } from "../../lib/site";

export async function getStaticPaths() {
  const entries = await getCollection("visuals", ({ data }) => isContentVisible(data.status));
  return entries.map((entry) => ({ params: { slug: slugFromId(entry.id) }, props: { entry } }));
}

interface Props {
  entry: CollectionEntry<"visuals">;
}

const { entry } = Astro.props;
const { Content, headings } = await render(entry);
---

<ArticleLayout entry={entry} section="Visuals" headings={headings}>
  <Content />
</ArticleLayout>
