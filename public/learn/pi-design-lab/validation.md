# Pi Design Lab · 验证范围

2026-09-07：本课程的十组流程、九道预测题、排序、参数实验、真实记录筛选与学习进度恢复经过浏览器检查。桌面和 320 / 390px 布局经过检查，三种展示均有窄屏覆盖。来源弹窗支持 Escape 和返回焦点，播放按钮支持键盘暂停。

2026-09-07 增补：新增的五张 SVG 图在 320 / 390 / 650 / 800 / 1280px 与三种展示下逐节检查，九节均无横向溢出，控制台无报错。会话树的叶子既可点图选择，也保留了图下的按钮；结构分布带可点击定位到对应行，条目列表仍是无障碍路径。流程的 ← → 与空格快捷键在焦点位于流程内时生效，输入框与下拉框保留原有键盘行为。

教学模型、图形几何与三个样本结构共通过 3,324 个断言，多数是逐条结构检查，不是 Pi 上游测试或 Provider 基准。图中的比例来自这些教学模型，不是实测。动画、预算、缓存收益与恢复计数为教学模拟。

减少动态效果与无 JavaScript 回退经过源码检查；SVG 图不含动画。未做屏幕阅读器、其他浏览器引擎和实际 file:// 测试；图形的 title / desc 未经辅助技术实测。笔记下载请求已触发，文件落盘与清除进度尚未完成验证。公开数据仅包含结构投影，不包含原始提示、Thinking 文本或工具输出正文。
